services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: cv-augmentor-frontend:latest
    ports:
      - "127.0.0.1:3000:3000" 
    environment:
      API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
      NODE_ENV: production
    restart: unless-stopped

